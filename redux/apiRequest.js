import {loginStart} from "./authSlice";

export const loginUser = (user, dispatch, navigate) => {
    dispatch(loginStart());
    try {
        const res = fetch()
    } catch (error) {
        
    }
};